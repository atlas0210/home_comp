/**
 * @typedef {Object} HomeRecord
 * @property {string} homeId
 * @property {string} name
 * @property {string} short
 * @property {string} status
 */

/**
 * @typedef {Record<string, Record<string, any>>} OverrideMap
 */

/**
 * @typedef {Record<string, number>} WeightMap
 */

/**
 * @typedef {{ title: string, fields: Array<{ key: string, label: string, type: string, options?: string[] }> }} EditGroup
 */

/**
 * @typedef {{ key: string, label: string, minWidth: number, mobileMinWidth: number, rawValue?: Function, scoreValue?: Function, sortValue?: Function }} ScoredFactorSpec
 */

export {};
