export const React = window.React;
export const ReactDOM = window.ReactDOM;
export const PropTypes = window.PropTypes;
export const Recharts = window.Recharts;
export const { useEffect, useMemo, useRef, useState } = React;
export const { ResponsiveContainer, BarChart, Bar, XAxis, YAxis, Tooltip, RadarChart, Radar, PolarGrid, PolarAngleAxis, Legend, Cell } = Recharts;
